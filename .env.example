### Example environment variables for docker-compose
# Copy this file to .env and fill with real secrets before running docker-compose

# Postgres
POSTGRES_DB=auditiq
POSTGRES_USER=postgres
POSTGRES_PASSWORD=replace_with_strong_password

# Backend
DATABASE_URL=postgresql://postgres:replace_with_strong_password@postgres:5432/auditiq
SECRET_KEY=replace_with_secret_key

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:8000
BETTER_AUTH_SECRET=replace_with_secret
